{
  "name": "irisub",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "dotenv -- turbo dev --parallel",
    "emulators": "firebase emulators:start",
    "build": "turbo build",
    "lint": "turbo lint",
    "migrate": "dotenv -- turbo migrate"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "dotenv-cli": "^7.1.0",
    "rome": "12.0.0",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.18.1"
}