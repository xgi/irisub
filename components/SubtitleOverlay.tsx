import styles from "../styles/components/SubtitleOverlay.module.scss";

type Props = {};

const SubtitleOverlay: React.FC<Props> = (props: Props) => {
  return <div className={styles.container}>something here</div>;
};

export default SubtitleOverlay;
